schema_version = "0.2"
name = "web-path-traversal-capsule"
version = "0.1.0"
type = "app"
default_target = "static"

[targets.static]
runtime = "web"
driver = "static"
entrypoint = "../secret"
port = 8080
