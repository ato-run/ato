schema_version = "1.0"
name = "test-concurrent-a"
version = "0.1.0"
description = "Test F: Concurrent deployment test - App A (Green)"
type = "app"

[execution]
runtime = "source"
entrypoint = "python3 server.py"
