# v1 compat: python, no deps, explicit readiness_probe.http_get.
schema_version = "0.3"
name = "compat-python-stdlib-explicit"
version = "0.1.0"
type = "app"
default_target = "web"

[targets.web]
runtime = "source"
run = "python3 app.py"
port = 8080
readiness_probe = { http_get = "/health" }
