schema_version = "0.3"
name = "ato-dock"
version = "0.1.0"
type = "app"
runtime = "web/static"
run = "dist"
port = 8080

[metadata]
description = "Ato developer dock"

build = "npm install && npm run build"
