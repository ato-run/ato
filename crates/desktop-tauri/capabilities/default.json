{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability. Scoped to the `main` window ONLY — the bundled first-party Ato UI (local asset). Remote origins and any future non-first-party webview must never receive these permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
