# Static Web Bundle Receipt JCS v1 fixture source

The receipt is derived from the normative `ato.static-web-manifest/v1` fixture
in this repository. Its byte identity is asserted by the Rust contract test.
The manifest provenance is recorded next to that source fixture.
