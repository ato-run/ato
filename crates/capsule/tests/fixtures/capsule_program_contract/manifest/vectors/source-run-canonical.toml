schema_version = "0.3"
name = "source-run"
version = "0.1.0"
type = "app"
default_target = "app"

[targets.app]
runtime = "source"
driver = "node"
run_command = "node server.js"
port = 8082

[services.main]
entrypoint = "node server.js"
