schema_version = "0.3"
name = "vite-build-output-layer"
version = "0.1.0"
type = "app"
default_target = "main"

[targets.main]
runtime = "source/native"
run = "./dist/run.sh"
build = "sh ./build.sh"

[build]
command = "sh ./build.sh"
outputs = ["dist"]
