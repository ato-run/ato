# NOTE: This sample uses the legacy IPC format which is not supported by the current ato-cli.
# The [ipc.imports] / [ipc.exports] feature has been removed from the manifest spec.
# This sample is preserved for historical reference only.
schema_version = "0.3"
name = "greeter-service"
version = "0.1.0"
type = "app"
runtime = "source/node"
run = "node server.js"
port = 50051

[metadata]
description = "Minimal IPC shared service — returns greetings via JSON-RPC 2.0"
author = "Capsule Examples"
