schema_version = "0.3"
name = "oci-alpine-hello"
version = "0.1.0"
type = "app"
default_target = "app"

[targets.app]
runtime = "oci"
image = "alpine:3.21"
run_command = "echo 'hello from alpine OCI runtime'"
