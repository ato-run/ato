{
  "name": "tauri-ambiguous-root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm --workspace desktop-a run build && npm --workspace desktop-b run build"
  }
}
