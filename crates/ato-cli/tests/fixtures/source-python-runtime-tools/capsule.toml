schema_version = "0.3"
name = "source-python-runtime-tools-fixture"
version = "0.1.0"
type = "app"
default_target = "main"

[targets.main]
runtime = "source"
driver = "python"
runtime_version = "3.11"
runtime_tools = { node = "20" }
build = "npm install --no-audit --fund=false && npm run build"
run = "python app/server.py"
port = 19876
