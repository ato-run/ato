schema_version = "0.3"
name = "installed-relaunch-node"
version = "0.1.0"
type = "app"
default_target = "main"

[targets.main]
runtime = "source"
driver = "node"
runtime_version = "22.14.0"
run = "node server.js"
port = 18880

[pack]
include = [
  "capsule.toml",
  "package.json",
  "package-lock.json",
  "server.js",
  "README.md",
]
