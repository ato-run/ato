[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "markitdown-pdf-helper"
version = "0.1.0"

[tool.setuptools]
packages = ["markitdown_pdf_helper"]