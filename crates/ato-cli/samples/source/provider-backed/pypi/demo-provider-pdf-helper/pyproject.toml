[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-provider-pdf-helper"
version = "0.1.0"

[tool.setuptools]
packages = ["demo_provider_pdf_helper"]