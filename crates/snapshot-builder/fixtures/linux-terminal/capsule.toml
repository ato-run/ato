schema_version = "0.3"
name = "ato-linux-terminal-fixture"
version = "0.1.0"
type = "app"
default_target = "terminal"

[targets.terminal]
runtime = "source"
language = "python"
run = "python3 terminal_fixture.py"
port = 8080
readiness_probe = { http_get = "/health" }

[targets.terminal.surface]
kind = "terminal"
profiles = ["ato.terminal-surface.v1"]
