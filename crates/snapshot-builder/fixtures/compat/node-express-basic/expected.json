{
  "class": "positive",
  "eligibility": "pass",
  "runtime": "node",
  "probe_synthesized": false,
  "start_cmd": "node server.js",
  "seal": "sealed",
  "manifest_source": "repo",
  "notes": "npm ci --omit=dev || npm install --omit=dev path (pinned express)."
}
