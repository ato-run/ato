# v1 compat NEGATIVE: port declared but NO run command — must fail eligibility (R7).
schema_version = "0.3"
name = "compat-missing-run"
version = "0.1.0"
type = "app"
default_target = "app"

[targets.app]
runtime = "source"
driver = "python"
port = 8080
