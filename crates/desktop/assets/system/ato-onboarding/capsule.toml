schema_version = "0.3"
name = "ato-onboarding"
version = "0.1.0"
type = "app"
runtime = "web/static"
run = "dist"
port = 8080

[metadata]
description = "Ato onboarding flow"

build = "npm install && npm run build"
