# Async Record Writer / RecordFrontier receipt — 2026-08-23

Status: implementation complete; Draft PR open

## Stack

- repository: `ato-run/ato`
- base: `feat/record-operation-model-v1`
- verified base head: `5d39069e7c8dfeb34c2aa25a1c8ac8a6f2274ba4`
- dependency: Draft PR `ato-run/ato#1292`
- branch: `feat/async-record-writer-frontier-v1`
- Draft PR: `https://github.com/ato-run/ato/pull/1293`

## Implemented boundary

- bounded, non-blocking Stylus submission;
- explicit Run failure on overflow, never silent drop;
- independent schema-validating Record Writer thread;
- writer-assigned global order;
- append-only `active.open` and immutable digest-keyed segments;
- segment count/order/length/payload-closure validation;
- published processed-through watermark;
- Capture Barrier with paused Stylus lease;
- sealed RecordFrontier with recomputed causal cut;
- frontier/segment reload from immutable closure;
- run-to-frontier association performed above the Writer;
- `ato.replay@2` frontier provenance and explicit v2 encap;
- v2 recording bypasses legacy per-Record head advancement.

## Focused verification

- Record Writer unit tests: PASS (8/8)
- operation replay/frontier CLI acceptance: PASS
- HTTP/PTy/Binding/CLI compile and focused tests: PASS
- focused clippy with `-D warnings`: PASS
- architecture check: PASS (22 workspace packages)
- `git diff --check`: PASS
- `cargo fmt --all -- --check`: PASS
- `cargo check --workspace --all-targets --locked`: PASS
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`: PASS
- `cargo run -p arch-check --locked`: PASS
- `cargo test -p ato-cli --test computation_architecture --locked -- --test-threads=1`: PASS (13/13)
- full workspace: all targets PASS except the two known macOS netd tests in the long worktree path (`SUN_LEN`)
- the same two netd tests at this exact head in the short `.tmp/a2s` worktree: PASS (2/2)

## Identity

No ComputationRef is accepted or generated by Record Writer APIs. Record ID,
segment digest, RecordFrontier digest, payload digest, and recording Adapter
provenance remain separate identities.

## Production

- inserted rows: 0
- updated rows: 0
- deleted rows: 0
- uploaded production objects: 0
- production approval requested: false
