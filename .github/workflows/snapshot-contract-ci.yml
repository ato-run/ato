name: snapshot provider

on:
  push:
    paths: ["extensions/providers/snapshot/**", "tools/snapshot-builder/**", "lib/**"]
  pull_request:
    paths: ["extensions/providers/snapshot/**", "tools/snapshot-builder/**", "lib/**"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo test -p ato-provider-snapshot -p snapshot-builder -p guest-agent
      - run: cargo clippy -p ato-provider-snapshot -p snapshot-builder -p guest-agent --all-targets -- -D warnings
